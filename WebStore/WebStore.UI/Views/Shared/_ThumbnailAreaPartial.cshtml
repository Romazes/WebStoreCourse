@model IEnumerable<MenuItem>

@if(Model.Count() > 0)
{
    <div class="col-12">
        <div class="row">
            <h3 class="text-success">@Model.FirstOrDefault().Category.Name</h3>
        </div>
    </div>
}